#!/usr/bin/env sage

from sage.all import *

p0 = var('p0')              # incident wave amplitude
R = var('R')                # numerical reflectance
x = var('x')                # x axis position
y = var('y')                # y axis position
w = var('w')                # frequency
c = var('c')                # speed of sound
t = var('t')                # time
k = var('k')                # discrete-domain wavenumber
theta = var('theta')        # angle
incident = var('incident')  # incident
